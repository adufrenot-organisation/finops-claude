# V87 — Génération HTML élégante intégrée

La mise en page validée sur le prototype HTML est désormais intégrée directement dans la génération FinOps.

## Ouvrir en HTML / rapport autonome
- mise en page plus aérée et plus compacte ;
- espace systématique entre libellés et montants ;
- ROI principal mieux hiérarchisé ;
- suppression du ROI dupliqué dans le bloc des coûts ;
- explication du coût fixe repliable ;
- vue budgétaire par domaine avec bouton `Voir détail` ;
- cartes Domaine repliées par défaut à l'écran ;
- ouverture directe du domaine depuis la synthèse ;
- à l'impression/PDF, tous les détails Domaine sont automatiquement visibles ;
- les détails équipes / pré-simulation restent intégrés dans le domaine correspondant.

La même présentation est utilisée pour le HTML d'un scénario et pour le rapport HTML de comparaison de plusieurs scénarios.

Aucune migration Grist supplémentaire n'est nécessaire.
